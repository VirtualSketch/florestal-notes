import { Box, Text } from 'native-base';

import { AppContainer } from '../core';

function UserPage() {
  return (
    <AppContainer>
      <Box>
        <Text>User Page</Text>
      </Box>
    </AppContainer>
  );
}

export default UserPage;
